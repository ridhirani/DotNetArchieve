using System;
using System.Collections;
using System.Collections.Generic;

namespace Queuecol
{
    class Program
    {
        static void Main(string[] args)
        { /*
            Console.WriteLine("************Queue<T>  Generic class collection********");
            //reprents the FIFO collection of objects
            Queue<string> numbers = new Queue<string>();
            numbers.Enqueue("Hi");
            numbers.Enqueue("bye");
            numbers.Enqueue("See");
            numbers.Enqueue("Ya");
            numbers.Enqueue("Tc");
            foreach (string item in numbers)
            {
                Console.WriteLine(item);
            }

            //it will print the first element
            Console.WriteLine(numbers.Peek());
            //it will delete th first element
            Console.WriteLine(numbers.Dequeue());
            //it will print the whlole stack
            foreach (string item in numbers)
            {
                Console.WriteLine(item);
            }
            Console.WriteLine(numbers.Count);
            Console.WriteLine(numbers.Contains("See")); // case sensiitive
            */
            /*
            //Creating as Queue
            Queue books = new Queue();
            //inserting values in queue
            books.Enqueue("Subtle Art of Not Giving Fuck");
            books.Enqueue("Atomic Theory");
            books.Enqueue("Rich Daddy Poor Daddy");
            books.Enqueue("Do Epic Shit");
            //displaying elements of queue
            Console.WriteLine("Orginal Queue");
            foreach (var item in books)
            {
                Console.WriteLine(item);
            }
            Console.WriteLine("***Before deletion****");
            Console.WriteLine("**The elemet to be deleted***" + books.Peek());
            Console.WriteLine(books.Dequeue());
            Console.WriteLine("***After Deletion***");
            foreach (var item in books)
            {
                Console.WriteLine(item);
            }
            Console.WriteLine(books.Count);
            Console.WriteLine(books.SyncRoot);
            Console.WriteLine(books.Contains("Do Epic Shit"));
           // Console.WriteLine(books.Clear());
        */
           
            }
    }
}
